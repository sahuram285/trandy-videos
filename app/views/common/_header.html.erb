<nav class="navbar navbar-expand-sm bg-light">
  <a class="navbar-brand" href="#">Video's</a>

  <!-- Links -->
  <ul class="navbar-nav">
    <li class="nav-item">
      <% if user_signed_in? %>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      <% end %>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="/">Home</a>
    </li>
    <% if user_signed_in? %>
      <li class="nav-item">
        <a class="nav-link" href="<%= my_videos_path %>">My Video's</a>
      </li>
    <% end %>
  </ul>
</nav>
